<?php
Page::open();
  Row::open();
  Col::open(["col"=>12]);  
   Card::open(["title"=>"Create Delivery"]);   
    Doc::open(["name"=>"create_delivery"]); 
   Card::close();
  Col::close();
  Row::close();
Page::close();
?>